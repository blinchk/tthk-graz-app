<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="tthk_graz_app.MainPage"
             Title="Kontaktid">

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="addContactToolbarItem"
            Text="Lisa"
            Order="Primary"
            Priority="0"
            Clicked="AddContactToolbarItem_OnClicked"/>
    </ContentPage.ToolbarItems>

    <StackLayout>
        <ListView x:Name="contactsList" ItemsSource="{Binding ContactList}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding FirstName}" FontSize="15"/>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
